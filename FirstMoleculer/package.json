{
  "name": "first-moleculer",
  "version": "1.0.0",
  "description": "This project is for learn the framework moleculer JS",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --config nodemon.json",
    "run-dev": "moleculer-runner --env --config moleculer.dev.config.js",
    "start": "moleculer-runner --env --config moleculer.prod.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jose Segura Nietzen",
  "license": "ISC",
  "dependencies": {
    "amqplib": "0.5.5",
    "axios": "0.21.1",
    "dotenv": "8.2.0",
    "moleculer": "0.13.12",
    "moleculer-repl": "0.6.2"
  },
  "devDependencies": {
    "nodemon": "2.0.2"
  }
}
